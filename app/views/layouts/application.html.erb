<!DOCTYPE html>
<html>
  <head>
    <title>Changer la vie - Opportunités pour s'engager et faire la différence</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="description" content="Découvrez des opportunités près de chez vous pour changer des vies : bénévolat, don du sang, éducation, entrepreneuriat social et plus encore.">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" />
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", defer: true %>
  </head>

  <body>
    <header class="bg-green-600 text-white shadow-md">
      <div class="container mx-auto px-4 py-5 flex flex-wrap items-center justify-between">
        <div class="flex items-center">
          <div class="text-white mr-3">
            <svg xmlns="http://www.w3.org/2000/svg" class="h-7 w-7" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
              <path stroke-linecap="round" stroke-linejoin="round" d="M17.657 16.657L13.414 20.9a1.998 1.998 0 01-2.827 0l-4.244-4.243a8 8 0 1111.314 0z" />
              <path stroke-linecap="round" stroke-linejoin="round" d="M15 11a3 3 0 11-6 0 3 3 0 016 0z" />
            </svg>
          </div>
          <%= link_to "Changer la vie", root_path, class: "text-2xl font-bold text-white" %>
        </div>

        <nav class="hidden md:flex items-center space-x-6">
          <%= link_to "Accueil", root_path, class: "font-medium text-white hover:text-yellow-100 transition" %>
          <%= link_to "Notre mission", mission_path, class: "font-medium text-white hover:text-yellow-100 transition" %>
          <%= link_to "Contact", contact_path, class: "font-medium text-white hover:text-yellow-100 transition" %>
        </nav>
      </div>
    </header>

    <main class="flex-1">
      <%= yield %>
    </main>
  </body>
</html>
